version: 2
models:
  - name: dim_creditcard
    description: Tabela de dimensão de cartões de crédito da AdventureWorks.
    columns:
      - name: ID_CreditCard
        description: Chave primária da tabela dim_creditcard.
        tests:
          - unique
          - not_null