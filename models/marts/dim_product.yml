version: 2
models:
  - name: dim_product
    description: Tabela de dimensão de produtos da AdventureWorks.
    columns:
      - name: ID_order
        description: Chave primária da tabela dim_product.
        tests:
          - unique
          - not_null